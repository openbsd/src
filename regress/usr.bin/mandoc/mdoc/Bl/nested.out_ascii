BL-NESTED(1)               OpenBSD Reference Manual               BL-NESTED(1)

NNAAMMEE
     BBll--nneesstteedd - nested lists

DDEESSCCRRIIPPTTIIOONN
     inset:

     outer list

     inner list
     inset compact:
     outer list
     inner list
     dash:

     --   outer list

         --   inner list
     dash compact:
     --   outer list
         --   inner list
     tag:

     outer tag
           outer text

           inner tag
                 inner text
           outer text

     outer tag
           outer text (display)

           inner tag
                 inner text
           outer text

     outer tag
                 outer text (one-line display)

           inner tag
                 inner text
           outer text
     final text

OpenBSD                        December 31, 2012                       OpenBSD
