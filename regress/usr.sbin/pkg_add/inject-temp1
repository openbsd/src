#! /usr/bin/perl

# $OpenBSD: inject-temp1,v 1.1 2019/07/09 13:49:47 espie Exp $
#use OpenBSD::Temp;
use strict;
use warnings;
use OpenBSD::Temp;

package OpenBSD::Temp;
use Wrapper;

wrap('permanent_dir', 
    sub {
	my $original = shift;
	$_[0] = "/nonexistent";
	goto &$original;
    });
1;
