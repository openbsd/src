#!/bin/sh
#
# $OpenBSD: amd,v 1.2 2011/07/08 04:29:54 ajacoutot Exp $

daemon="/usr/sbin/amd"

. /etc/rc.d/rc.subr

rc_reload=NO
rc_restart=NO
rc_stop=NO

rc_pre() {
	[ -e ${amd_master} ] || return 1
	daemon_flags=$(cat ${amd_master})
}

rc_cmd $1
