

	This directory contains unsuppored contributions 
to smtpd that people have found useful.

	smtpfilter.c	outgoing filter used to remove internal header
			lines from messages. You must set MAXARGS in the
			makefile to 5 to use this, as this is intended to 
			be invoked per-recipient. Written by Andre Albsmeier
			(Andre.Albsmeier@mchp.siemens.de). 
			
				
