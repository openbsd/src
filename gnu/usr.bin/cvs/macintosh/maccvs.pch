#ifdef powerc
  #pragma precompile_target "cvspch.PPC"
#else
  #pragma precompile_target "cvspch.68K"
#endif

#define __dest_os	__mac_os		// Necessary for MSL

#define HAVE_CONFIG_H			// Tells CVS source to look at config.h

#define MACOS						// Necessary for Z Library
